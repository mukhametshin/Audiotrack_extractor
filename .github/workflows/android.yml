name: Android CI (build APK)

on:
  workflow_dispatch:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      ANDROID_SDK_ROOT: /usr/local/lib/android/sdk
      ANDROID_HOME: /usr/local/lib/android/sdk
      ORG_GRADLE_CONSOLE: plain

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Ensure cmdline-tools (sdkmanager)
        shell: bash
        run: |
          set -euxo pipefail
          SDK="$ANDROID_SDK_ROOT"
          SDKMAN="$SDK/cmdline-tools/latest/bin/sdkmanager"
          mkdir -p "$SDK/cmdline-tools"
          if [ ! -x "$SDKMAN" ]; then
            curl -fsSL https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip -o /tmp/cmd.zip
            rm -rf /tmp/cmd && mkdir -p /tmp/cmd
            unzip -q /tmp/cmd.zip -d /tmp/cmd
            mkdir -p "$SDK/cmdline-tools/latest"
            mv /tmp/cmd/cmdline-tools/* "$SDK/cmdline-tools/latest/"
          fi
          "$SDK/cmdline-tools/latest/bin/sdkmanager" --version

      - name: Accept Android SDK licenses (non-interactive)
        shell: bash
        run: |
          set -euo pipefail
          SDK="$ANDROID_SDK_ROOT"
          SDKMAN="$SDK/cmdline-tools/latest/bin/sdkmanager"
          { for i in {1..10}; do printf 'y\n'; done; } | "$SDKMAN" --licenses --sdk_root="$SDK" >/dev/null || true

      - name: Install Android SDK packages
        shell: bash
        run: |
          set -euxo pipefail
          SDK="$ANDROID_SDK_ROOT"
          SDKMAN="$SDK/cmdline-tools/latest/bin/sdkmanager"
          "$SDKMAN" --sdk_root="$SDK" \
            "platform-tools" \
            "platforms;android-35" \
            "build-tools;35.0.0" || true

      - name: Build (clean assembleDebug)
        uses: gradle/gradle-build-action@v2
        with:
          gradle-version: 8.9
          arguments: clean assembleDebug --stacktrace --info --warning-mode all

      - name: Upload debug APK
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: app-debug-apk
          path: app/build/outputs/apk/debug/*.apk
